import { createFileRoute } from "@tanstack/react-router";

export const Route = createFileRoute("/")({
  head: () => ({
    meta: [
      { title: "SwingSense AI — Your AI golf swing coach" },
      { name: "description", content: "Record your golf swing and get instant on-device AI analysis." },
    ],
  }),
  component: () => null, // overridden by ./index.tsx with same path? No — index.tsx is the real one
});
